import * as React from "react";
import { bindActionCreators } from "redux";

export interface ReduxProps {}

export default class Redux extends React.Component<ReduxProps, {}> {
  public render() {
    return <div />;
  }
}
